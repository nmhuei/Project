

# This file was *autogenerated* from the file A_Twisted_Mind.sage
from sage.all_cmdline import *   # import sage library

_sage_const_2 = Integer(2); _sage_const_192 = Integer(192); _sage_const_237 = Integer(237); _sage_const_3 = Integer(3); _sage_const_1379137549983732744405137513333094987949371790433997718123 = Integer(1379137549983732744405137513333094987949371790433997718123); _sage_const_6277101735386680763835789423072729104060819681027498877478 = Integer(6277101735386680763835789423072729104060819681027498877478); _sage_const_1 = Integer(1); _sage_const_13416 = Integer(13416); _sage_const_0 = Integer(0)
from sage.all import *
from Pwn4Sage.pwn import remote
import json

p = _sage_const_2 **_sage_const_192  - _sage_const_237 
a = -_sage_const_3 
b = _sage_const_1379137549983732744405137513333094987949371790433997718123 
order = _sage_const_6277101735386680763835789423072729104060819681027498877478 

# tìm d sao cho Q = G * d (mod order(G))
def pohlig_hellman(Q, G):
    d_remainders = []
    small_modulus = []

    n = G.order()
    factors = factor(n)[:-_sage_const_1 ] # bỏ đi ước cuối nhé
    
    for q, e in factors:
        # nhóm con của Q và G
        small_Q = Q * (n // (q**e))
        small_G = G * (n // (q**e))
        
        small_d = discrete_log(small_Q, small_G, operation = "+")
        
        d_remainders.append(small_d)
        small_modulus.append(q**e)

    d = crt(d_remainders, small_modulus)
    big_modulus = prod(small_modulus)
    
    return d, big_modulus

# Đường cong E GF(p)
E = EllipticCurve(GF(p), [a, b])
# Twist của E

non_square = -_sage_const_1 
assert Mod(non_square, p).is_square() == false
twist = E.quadratic_twist(non_square)
# Đường cong E trên GF(p^2)
E2 = EllipticCurve(GF((p, _sage_const_2 ), "k"), [a, b])


# giờ ta tìm hai điểm P và Q có order bằng với order của E và twist
P = E2.lift_x(ZZ(randint(_sage_const_1 , p)))
while P.order() != E.order():
    P = E2.lift_x(ZZ(randint(_sage_const_1 , p)))

Q = E2.lift_x(ZZ(randint(_sage_const_1 , p)))
while Q.order() != twist.order():
    Q = E2.lift_x(ZZ(randint(_sage_const_1 , p)))

print(f"{P = }")
print(f"{Q = }")

io = remote("socket.cryptohack.org", _sage_const_13416 )
io.recvuntil(b'You have 120 seconds to submit the private key in decimal format.\n')

d_res = []
moduli = []
# Giờ ta gửi P và Q để thực hiện Pohlig hellman
for point in [P, Q]:
    payload = {"option": "get_pubkey", 'x0': int(point[_sage_const_0 ])}
    io.sendline(json.dumps(payload).encode())
    public_key = json.loads(io.recvline().decode())
        
    # Vì yA chỉ được lấy một trường hợp nên giá trị d có thể bị đổi dấu 
    A = E2.lift_x(ZZ(public_key["pubkey"]))
    
    # A = B * d
    d_remainders, modulus = pohlig_hellman(A, point)
    d_res.append(d_remainders)
    moduli.append(modulus)

# giờ ta sẽ CRT hệ sau là có d, nhưng phải chia 4 trường hợp ra nhé
# d = d_res[0] (mod moduli[0])
# d = d_res[1] (mod moduli[1])

# d = -d_res[0] (mod moduli[0])
# d = d_res[1] (mod moduli[1])

# d = d_res[0] (mod moduli[0])
# d = -d_res[1] (mod moduli[1])

# d = -d_res[0] (mod moduli[0])
# d = -d_res[1] (mod moduli[1])

# 4 trường hợp thì cũng ít, nhưng mà mình code cái này cho nó tổng quát lun, sau này dùng lại
from itertools import product
sign_remainders = []
for signs in product([_sage_const_1 , -_sage_const_1 ], repeat=len(d_res)):
    signed = [sign * val for sign, val in zip(signs, d_res)]
    sign_remainders.append(signed)

private_key = []
for d_r in sign_remainders:
    private_key.append(crt(d_r, moduli))
    
for i in private_key:
    payload = {"option": "get_flag", "privkey": int(i)}
    io.sendline(json.dumps(payload).encode())
io.interactive()

